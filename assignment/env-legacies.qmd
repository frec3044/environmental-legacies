---
title: "The ecological consequences of redlining"
author: "YOUR NAME"
format: 
  html:
    embed-resources: true
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(messages = FALSE, cache = FALSE)
library(rstac)
library(gdalcubes)
library(stars)
library(tmap)
library(tidyverse)
gdalcubes::gdalcubes_options(parallel = TRUE)
```
## GitHub link

Provide link to your assignment GitHub repository here:

## Learning objectives

This module provides an introduction to the fundamentals of working with spatial vector and raster data in R while empirically exploring how a past systematic and structural racist policy is intertwined with urban ecological processes. It uses the Simple Features Access standard (ISO 19125) and a tidyverse-style workflow using the sf package and an emerging ecosystem of r-spatial tools.

## Background

In August 2020, [Christopher Schell](https://ourenvironment.berkeley.edu/users/1757114) and colleagues published a review in *Science* on ['The ecological and evolutionary consequences of systemic racism in urban environments'](https://www.science.org/doi/10.1126/science.aay4497) (DOI: 10.1126/science.aay4497), showing how racism and classism has significant impacts on ecological and evolutionary processes within urban environments. Here we explore a subset of the data used to support these findings in this review and the broader literature.

**Figure 2** in the Schell paper shows how NDVI (Normalized Difference Vegetation Index) tracks historical redlining.

![Fig. 1.](figures/fig2.png)

We are going to create maps like these and plot the distributions and mean vegetation patterns across cities to explore the legacy of redlining.

To do this, we are going to use the following spatial data:

#### Mapping Inequality: (vector data)

Please take the time to read the introduction to this dataset [here](https://dsl.richmond.edu/panorama/redlining/#loc=3/41.245/-105.469&text=intro)

#### Normalized Difference Vegetation Index (NDVI) (raster data)

NDVI is used as a proxy measure of vegetation health, cover, and phenology (life cycle stage) over large areas. It is calculated using multiple bands from satellite images.

## Warm-up questions

**Question 1:**

Using the `tutorial/env-legacies-tutorial.qmd` as the template, create a map that shows the current mean NDVI within each grade class polygon in Richmond, VA.

Tip: one challenge is getting the `xmin`, `ymin`, `xmax`, and `ymax` in the `box` correct.  It needs to be large enough to include the entire city.

Tip: Use Google Maps (click on a location) to find longitude and latitude coordinates that mark the bounds of a box that surrounds the city.

Tip: Richmond, VA is centered around 37.54°N, 77.43°W. A starting bounding box of roughly `xmin = -77.6, ymin = 37.4, xmax = -77.3, ymax = 37.7` covers the city — adjust as needed to match the extent of the redlining polygons.

**Answer 1:**

```{r}

```

**Question 2:**

Create a table that displays the mean NDVI for the different grade classifications within Richmond, VA. (one value per grade.)

**Answer 2:**

```{r}

```

## Examining how the influence of redlining varies across city environmental or demographic characteristics

In this section, you are going to examine how the relative difference between the highest grade and lowest grade differs between cities. You will then explore how that difference is related to environmental or demographic differences among the cities.

**Question 3:**

Read in the characteristics of the different cities in the dataset. The characteristics are in the CSV called `assignment/data/city_data.csv`. City data was generated by combining <https://simplemaps.com/data/us-cities> with daymet meteorology data (<https://daymet.ornl.gov>)

**Answer 3:**

```{r}

```

The data has the following columns

```
Rows: 198
Columns: 14
$ city       <chr> City name (note that city name is not unique because the same name can be in multiple states)
$ state      <chr> State
$ precip     <dbl> annual precipitation (mm / yr)
$ range      <dbl> Range in annual temperature (°C)
$ tavg       <dbl> mean annual temperature (°C)
$ solar      <dbl> mean daily solar radiation (W/m2)
$ tmax       <dbl> mean max temperature (°C)
$ tmin       <dbl> mean min temperature (°C)
$ vpd        <dbl> mean vapor pressure deficit (kPa)
$ lat        <dbl> Latitude (degrees)
$ lng        <dbl> Longitude (degree)
$ population <dbl> Population
$ density    <dbl> Population density (individual / km2)
$ ranking    <dbl> An integer from 1-5 that captures the importance of a city (1 is most important, 5 least important).
```

**Question 4:**

Pick **three** cities that span a range of environmental or demographic characteristics. 

Select one variable in the data set from Question 3 that 1) interests you and 2) has a large enough range where you see differences across your three selected cities. 

List your three cities, the focal variable, and the range of your variable across your three cities.  Be sure the range of your variables is shown in your rendered HTML.

**Answer 4:**

```{r}

```

**Question 5:**

How do you expect the relationship between grade and NDVI to change across your selected environment or demographic variable? Why do you expect this pattern?  Be sure your answer specifically address why you expect *the difference in NDVI between A and D grades* to be different across the selected environment or demographic variable.

**Answer 5:**


**Question 6:**

Generate a map of NDVI within each class for each city (three separate plots) and a table with mean ndvi by grade for the city

Notes:

- **Start early** — downloading and processing data can take many minutes per city depending on city size and your internet connection.
- Rendering a completed assignment took ~20 minutes on a typical connection.
- After an initial test run, reduce your bounding box to fit the city boundaries more tightly. Cities have grown since the 1930s, so the redlining polygons are smaller than the modern city footprint. A tighter box means less data to download and faster rendering.
- To further speed things up, try increasing `dx` and `dy` in `cube_view()` (units are decimal degrees of latitude/longitude). Don't increase them too much — NDVI resolution should stay finer than individual polygon sizes or you will have multiple polygons within a single NDVI pixel.
- Error `replacement has N rows, data has M rows`: your bounding box is smaller than the extent of the redlining polygons. Expand the box.
- Error `chunking[1] > 0 ... is not TRUE`: the box coordinates are likely invalid (e.g., `xmin`/`xmax` or `ymin`/`ymax` are swapped).

**Answer 6:**

City 1 Map

```{r}

```

City 1 Table: Mean NDVI by grade (save it as a unique data frame name)

```{r}

```

City 2 Map

```{r}

```

City 2 Table: Mean NDVI by grade (save it as a unique data frame name)

```{r}

```

City 3 Map

```{r}

```

City 3 Table: Mean NDVI by grade (save it as a unique data frame name)

```{r}

```

**Question 7:**

Describe any visual differences among the cities in the patterns of NDVI across the classes.

**Answer 7:**


**Question 8:**

Combine (bind) the data frames for the three cities into a single data frame. Filter out the NA grade.

Note: Some polygons in the redlining dataset were assessed but not assigned an A–D letter grade; these appear as `NA` in the `grade` column. Remove them so they don't affect your downstream plots and calculations.

**Answer 8:**

```{r}

```

**Question 9:**

Plot the values for the different classes for the three cities on the same graph (x-axis is grade, y-axis is mean NDVI, color is city).  Be sure to think about what geom is most appropriate?

**Answer 9:**

```{r}

```

**Question 10:**

Calculate the NDVI difference between the lowest (D) and highest class (A) for each city (do not use the NA class).  Print your results as a table in the HTML.  You will use a `pivot_` to answer this question.

**Answer 10:**

```{r}

```

**Question 11:**

Use a plot to answer the question: Does the NDVI difference between the highest and lowest classes vary with the environmental or demographic characteristic you choose in Question 3?  You will need to use joins to combine your NDVI difference table with the city environmental/demographic table.

Hint: City names are not unique across states (see the column descriptions in Question 3). Be sure to join on both `city` and `state` to avoid duplicate rows.

**Answer 11:**

```{r}

```
**Question 12:** 

Describe the relationship between your environmental or demographic variable and the NDVI difference across grades

**Answer 12:**

## Render and committing

Remember to Render your document as an `HTML` and comment+push to GitHub your code and rendered HTML document.  Use at least three commits that you push to GitHub. 

## Attribution

Include citations of any AI-generated assistance or discussion with classmates (per policy in the syllabus). Proper documentation of AI-generated assistance includes the prompt, the source (e.g., ChatGPT), and the significant parts of the response.  Proper documentation of discussion with classmates includes listing their names and the components discussed.  

## Citation

This tutorial is an extension of the tutorial created by Carl Boettiger and Millie Chapman at the University of California - Berkeley. <https://boettiger-lab.github.io/nasa-topst-env-justice/tutorials/R/1-intro-R.htm> and <https://github.com/espm-157/spatial-template>

